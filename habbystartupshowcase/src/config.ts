/**
 * ============================================================================
 * 个人主页配置文件
 * ============================================================================
 *
 * 🔴🔴🔴 重要！部署前必须修改 OWNER_EMAIL！🔴🔴🔴
 *
 * OWNER_EMAIL 用于保护私人页面（/private），确保只有创建者本人才能访问。
 * 这是一个必须遵守的安全规则，不是可选项！
 *
 * 如果不修改：
 * - 默认值是 'developer@habby.com'
 * - 只有这个邮箱的用户才能访问你的私人页面
 * - 你自己将无法访问自己的私人页面！
 *
 * ============================================================================
 */

export const config = {
  /**
   * 🔐 创建者邮箱（私人页面访问控制）
   *
   * ⚠️⚠️⚠️ 必须修改为你自己的 Habby 邮箱！⚠️⚠️⚠️
   *
   * 这个邮箱决定了谁可以访问 /private 页面：
   * - 只有邮箱匹配的登录用户才能访问私人页面
   * - 其他用户访问会看到"无权访问"提示
   * - 这是保护你的敏感个人信息（HR 档案）的关键
   *
   * 如何填写：
   * 1. 填写你在 Habby 系统中的工作邮箱
   * 2. 必须与你登录 apps.habby.com 时使用的邮箱一致
   * 3. 区分大小写，请确保完全一致
   *
   * 示例：
   * ✅ 正确：'zhangsan@habby.com'
   * ✅ 正确：'alice.wang@habby.com'
   * ❌ 错误：留空
   * ❌ 错误：使用默认值 'developer@habby.com'
   */
  OWNER_EMAIL: 'hengzihan@habby.com',  // 👈👈👈 必须修改为你自己的邮箱！

  /**
   * 页面标题和描述（可选）
   */
  SITE_TITLE: '我的个人主页',
  SITE_DESCRIPTION: '这是我的个人主页'
};
